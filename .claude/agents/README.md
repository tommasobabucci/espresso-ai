# .claude/agents/

Subagent definitions for espresso.ai. Each agent is a `.md` file with YAML frontmatter and a system prompt.

See `documentation/claude_docs/SUBAGENTS_GUIDE.md` for reference on agent configuration.
